{
  "name": "cockpit-security",
  "description": "Security tooling for a Cockpit module",
  "license": "LGPL-2.1-or-later",
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "build": "node ./build.js",
    "watch": "node ./build.js --watch"
  },
  "dependencies": {
    "@patternfly/patternfly": "6.1.0"
  }
}
