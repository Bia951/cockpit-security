{
  "name": "cockpit-security",
  "version": "0.1.0",
  "description": "A Cockpit security module for firewall and fail2ban operations",
  "type": "module",
  "main": "build.js",
  "license": "LGPL-2.1",
  "scripts": {
    "build": "node ./build.js",
    "watch": "node ./build.js -w"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.25.4"
  }
}
